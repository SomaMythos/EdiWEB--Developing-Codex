# URL base da API consumida pelo frontend (com /api no final).
# Com Cloudflare Tunnel ou proxy local, use /api para manter tudo no mesmo dominio.
VITE_API_URL=/api

# URL do backend usada apenas pelo proxy do Vite durante o desenvolvimento local.
VITE_BACKEND_URL=http://127.0.0.1:8000
